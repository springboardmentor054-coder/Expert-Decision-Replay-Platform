# Frontend Testing

This directory contains frontend tests for the Expert Decision Replay Platform.

## Running Tests

- `npm test` - Run all tests
- `npm run test:ui` - Run tests with UI
- `npm run test:coverage` - Run tests with coverage report

## Test Structure

- `api.test.ts` - Tests for API integration
- `App.test.tsx` - Tests for the main App component
